# WARNING
Add here the 2 files generated by the following commands:

## Generate self signed key
``` bash
openssl ecparam -genkey -name prime256v1 -out selfsigned.key
```

## Generate certificate with key
``` bash
openssl req -new -x509 -key selfsigned.key -out selfsigned.crt -days 3650
```